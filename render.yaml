services:
  # Backend Service
  - type: web
    name: shiftwise-2
    env: python
    buildCommand: |
      cd backend
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
    startCommand: |
      cd backend
      gunicorn --chdir backend shiftwise_backend.wsgi:application --log-file -
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: True
      - key: ALLOWED_HOSTS
        value: shiftwise-2.onrender.com,shiftwise-1.onrender.com
      - key: DATABASE_URL
        fromDatabase:
          name: shiftwise-db-test
          property: connectionString
    healthCheckPath: /api/health/
    plan: free

  # Frontend Service
  - type: web
    name: shiftwise-1
    env: node
    buildCommand: |
      cd Shedular@@
      npm install
      npm run build
    startCommand: |
      cd Shedular@@
      npm start
    envVars:
      - key: VITE_BACKEND_URL
        value: https://shiftwise-2.onrender.com
      - key: NODE_VERSION
        value: 18.0.0
    healthCheckPath: /
    plan: free

databases:
  - name: shiftwise-db-test
    databaseName: shiftwise_test
    user: shiftwise_test
    plan: free 